//=============================================================================
// ZoneUnion: Tell editor to merge same zone properties to this zone from
// a matching source zone by Tag.
// ZoneInfo.Tag must match this actor Event.
//=============================================================================
class ZoneUnion extends ZoneInfo
	native
	editoronly;

